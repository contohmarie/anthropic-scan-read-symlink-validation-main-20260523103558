# scan-plugins Read Symlink GitHub Actions Validation

This tester-owned repository validates the PoC on GitHub Actions `ubuntu-latest`.

The workflow does not call Anthropic production. The PoC starts a local fake Anthropic-compatible API and uses a dummy API key.
